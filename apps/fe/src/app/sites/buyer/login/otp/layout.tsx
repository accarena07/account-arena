import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Verifikasi OTP | GameMarket",
};

const BuyerOtpLayout = ({ children }: { children: React.ReactNode }) => {
  return children;
};

export default BuyerOtpLayout;
