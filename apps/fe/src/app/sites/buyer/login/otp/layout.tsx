import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Verifikasi OTP | GameMarket",
};

export default function BuyerOtpLayout({ children }: { children: React.ReactNode }) {
  return children;
}
