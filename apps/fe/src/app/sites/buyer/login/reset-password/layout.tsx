import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Buat Kata Sandi Baru | GameMarket",
};

const BuyerResetPasswordLayout = ({ children }: { children: React.ReactNode }) => {
  return children;
};

export default BuyerResetPasswordLayout;
