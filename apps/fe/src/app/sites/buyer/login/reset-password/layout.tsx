import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Buat Kata Sandi Baru | GameMarket",
};

export default function BuyerResetPasswordLayout({ children }: { children: React.ReactNode }) {
  return children;
}
